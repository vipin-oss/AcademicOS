# AcademicOS backend — runtime dependencies
# Stack (frozen): FastAPI · PostgreSQL · Qdrant · JWT
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
sqlalchemy==2.0.35
psycopg2-binary>=2.9.9
qdrant-client==1.11.0
pyjwt==2.9.0
bcrypt==5.0.0
python-multipart==0.0.12
httpx==0.27.2

# Intake v2 — deterministic extraction engines (M2 Part 1). Pure-python,
# pinned; no network/AI services behind them.
pypdf==5.1.0
python-docx==1.1.2

# L2 — document intelligence engines (ADR-028). Pure-python, pinned, MIT
# licensed; no network/AI services. pdfplumber (MIT) for PDF text/tables/
# regions; openpyxl (MIT) for XLSX; python-pptx (MIT) for PPTX; Pillow
# (MIT-CMU) for images. OCR is OPTIONAL and OFF by default (ADR-030) — it is
# NOT installed here; enable via the port behind a feature flag.
#
# OCR (Q2 resolved at V3 M4, ADR-052): Tesseract with the eng+hin language
# pack is the chosen engine (Devanagari-capable, no paid/proprietary service).
# To enable: install the tesseract binary + the hin traineddata, then
# uncomment the pytesseract pin below.
# pytesseract==0.3.13
pdfplumber==0.11.10
openpyxl==3.1.5
python-pptx==1.0.2
Pillow==12.3.0

# Migrations (alembic upgrade head)
alembic==1.13.2

# Development / tooling
pytest==8.3.3
ruff==0.6.9
